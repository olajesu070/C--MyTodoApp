@model string

@{
    var currentView = Model;
}

<div class="sidebar bg-secondary h-100">
    <ul class="nav flex-column">
       
        <li class="nav-item">
            <a class="nav-link text-white @(currentView == "Index" ? "bg-warning" : "")" asp-area="" asp-controller="Todos" asp-action="Index">View Todos</a>
        </li>
        <li class="nav-item">
            <a class="nav-link text-white @(currentView == "Create" ? "bg-warning" : "")" asp-area="" asp-controller="Todos" asp-action="Create">Create Todo</a>
        </li>
        <!-- Add other sidebar items as needed -->
    </ul>
</div>
